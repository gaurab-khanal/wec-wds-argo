apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nginx-multi-daemonset
  namespace: default
  labels:
    app.kubernetes.io/name: nginx-multi
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: nginx-multi
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nginx-multi
    spec:
      containers:
      - name: nginx
        image: nginx:1.26
        ports:
        - containerPort: 80
