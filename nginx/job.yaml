apiVersion: batch/v1
kind: Job
metadata:
  name: suspended-job
spec:
  suspend: true
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: test
        image: busybox
        command: ["echo", "hello"]
